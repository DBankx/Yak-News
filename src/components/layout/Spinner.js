import React from 'react';
import spinner from '../../imgs/spinner.gif';

const Spinner = () => {
  return (
    <div>
      <img src={spinner} alt='spinner' width='50' height='50' />
    </div>
  );
};

export default Spinner;
